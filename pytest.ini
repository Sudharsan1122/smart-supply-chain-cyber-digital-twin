[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    integration: full E2E tests requiring live DB + API
    performance: latency / throughput baselines
    slow: tests > 1 second
